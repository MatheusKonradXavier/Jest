describe('TESTANDO SHOW', () => {
  it('descrição do teste(IT)', () => {
    const number = 1;
    expect(number).not.toBe(1);
  });
});

describe('TESTANDO SHOW', () => {
  test('descrição de teste (TESTE)', () => {
    const nome = 'konrad';
    expect(nome).not.toBe('konrad');
  });
});
